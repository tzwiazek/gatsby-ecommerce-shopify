export interface HeaderSwipeInterface {}
export interface SwipeWrapInterface {}

export interface NotificationInterface {
  showMore: boolean;
}

export interface TextInterface {
  size?: string;
}

export interface NotificationLinkInterface {}
export interface ArrowInterface {
  type: 'left' | 'right';
}

export interface ArrowIconInterface {
  showMore?: boolean;
}
